# Notes Frontend environment variables
#
# Backend REST API base URL.
# For local dev it might be: http://localhost:3001
# In the hosted environment this should point to the notes_backend container URL.
NEXT_PUBLIC_NOTES_API_BASE_URL=http://localhost:3001
